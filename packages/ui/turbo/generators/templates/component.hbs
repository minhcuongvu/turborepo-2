export const {{ pascalCase name }} = ({ children }: { children: React.ReactNode | any }) => {
  return (
    <>
      <div className="{{ kebabCase name }}">
        <h1>{{ pascalCase name }} </h1>
        {children}
      </div>
    </>
  );
};
